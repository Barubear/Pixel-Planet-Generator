# Language packs

The built-in complete translations are stored in `../js/locales.js` so that the page can be opened directly with `file://`.

To add a language for every visitor, copy either JSON file, translate all keys, keep `language.name`, and add the complete object to `../js/locales.js` before deployment.
